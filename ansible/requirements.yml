---
# Ansible Galaxy requirements
# Install with: ansible-galaxy install -r requirements.yml

roles: []
  # Example external roles (uncomment if needed):
  # - name: geerlingguy.docker
  #   version: 6.1.0

collections:
  - name: kubernetes.core
    version: ">=2.4.0"

  - name: community.general
    version: ">=7.0.0"

  - name: ansible.posix
    version: ">=1.5.0"
